{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the agent's setup and diagnostics windows.",
  "windows": ["setup", "diagnostics"],
  "permissions": ["core:default", "dialog:default"]
}
