{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for local UI pages including setup, diagnostics, and secondary monitor blackout overlays.",
  "windows": ["setup", "diagnostics", "blackout*"],
  "permissions": ["core:default", "dialog:default"]
}
