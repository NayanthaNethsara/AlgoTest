# Server-only base URL for the Go backend (never exposed to the browser).
API_URL=http://localhost:8080

# Set true only when the site is served over HTTPS (online rounds).
# Keep false on an offline HTTP LAN, or the session cookie won't be sent.
COOKIE_SECURE=false
